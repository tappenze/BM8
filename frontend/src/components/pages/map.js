import React from "react";
import { Container, Form } from "react-bootstrap";
import "./style.css";

export default class Map extends React.Component {
    constructor(props) {
        super(props);
    }

    async componentDidMount() {

    }

    render() {
        return(<div><p>The map page</p></div>);
    }
}